name: Basic Pipeline Test
on: [push]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Print success
      run: echo "Basic workflow structure works!"
